实验1：模型参数量的选择

待选模型：yolo11n / yolo11s / yolo11m / yolo11l

评价指标：精度 + 成本

1. 精度：mAP50 / mAP5095
2. 成本：
   1. Params / GFLOPs
   2. 首先检查 preprocess time / inference time / postprocess time，如果前处理和后处理的时间都差不多，就只比较 inference time
   3. latency [ms] = preprocess time + inference time + postprocess time
   4. FPS = 1[s] / latency[ms]



实验2：基于预训练权重的微调 or 从头训练

评价指标：仅精度



实验3：模型imgsz的选择

待选值：640=20\*32 / 800=25\*32 / 1024=32\*32

评价指标：精度 + 成本

1. 精度
2. 成本



实验4：训练时是否使用cos_lr

评价指标：精度